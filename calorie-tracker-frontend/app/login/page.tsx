export default function LoginPage() {
  return (
    <section className="mx-auto flex max-w-6xl justify-center px-6 py-20">
      <div className="w-full max-w-md rounded-lg border border-line bg-white p-8 shadow-hairline">
        <h1 className="text-lg font-semibold tracking-tight text-ink">Sign in</h1>
        <p className="mt-2 text-sm leading-6 text-ink-muted">
          Authentication against the identity service is not wired yet. Use Log in
          in the header as a placeholder until the login form is added.
        </p>
      </div>
    </section>
  );
}
